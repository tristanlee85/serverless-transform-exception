import React from 'react';

const EdgioError = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '50px' }}>
      You were redirected here because of an error in the cloud function.
    </div>
  );
};

export default EdgioError;
